<h1>個人情報の詳細</h1>

<div class="nav justify-content-end">
<%= link_to  '新規登録', '/tasks/new', class: 'btn btn-primary' %>
</div>

<table class="table">
  <tbody>
  	<tr>
     <th scope="col-md-4">ID</th>	
     <td scope="col-md-4"><%= @task.id %></td>
    </tr>
    <tr>	
     <th scope="col-md-4">名前</th>
     <td scope="col-md-4"><%= @task.name %></td>
    </tr>
     <th scope="col-md-4">説明</th>
     <td scope="col-md-4"><%= @task.description %></td>
    </tr>
    <tr>
     <th scope="col-md-4">登録日時</th>
	 <td scope="col-md-4"><%= @task.created_at %></td>
    </tr>
  </tbody>
 <% end %>
